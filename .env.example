DATABASE_URL=postgresql://postgres:postgres@localhost:5433/support_ai?schema=public
REDIS_URL=redis://localhost:6379
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3.1
OLLAMA_EMBED_MODEL=nomic-embed-text
API_PORT=3001
WORKER_PORT=3002
NEXT_PUBLIC_API_URL=http://localhost:3001/v1
JWT_ACCESS_SECRET=secret
JWT_REFRESH_SECRET=secret
RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM=Support AI <onboarding@resend.dev>
# Use `console` for local dev without a verified domain — invite/verify
# links are printed in the worker logs instead of sent via Resend.
EMAIL_PROVIDER=console
APP_WEB_URL=http://localhost:3000

# Stripe (Test Mode) — create Hobby/Pro Products + monthly Prices in Dashboard
STRIPE_SECRET_KEY=sk_test_xxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx
STRIPE_HOBBY_PRICE_ID=price_xxxxxxxx
STRIPE_PRO_PRICE_ID=price_xxxxxxxx
# Checkout redirects use APP_WEB_URL + /dashboard/{slug}/settings/...
