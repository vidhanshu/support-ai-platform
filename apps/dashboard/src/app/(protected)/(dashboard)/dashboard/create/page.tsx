"use client";

import CreateWorkspaceCard from "@/components/dashboard/create-workspace-card";

export default function CreateWorkspacePage() {
  return (
    <main className="min-h-[calc(100vh-68px)] flex items-center justify-center">
      <CreateWorkspaceCard />
    </main>
  );
}
